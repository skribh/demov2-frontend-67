import React from 'react';
import { Layout } from '@/components/Layout';
import { PricingSection } from '@/components/investor/sections/PricingSection';

const Pricing: React.FC = () => {
  return (
    <Layout>
      <PricingSection />
    </Layout>
  );
};

export default Pricing;